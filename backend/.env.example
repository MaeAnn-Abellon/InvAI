# Backend environment example (DO NOT COMMIT real secrets in .env)
PORT=5000
# Full Postgres connection string (Neon/Supabase/etc.)
DATABASE_URL=postgresql://USERNAME:PASSWORD@HOST/DATABASE?sslmode=require
# Long random string (48+ chars hex recommended)
JWT_SECRET=change_me_to_long_random_secret_value
# Frontend origin (no trailing slash). Add multiple origins separated by comma if needed.
CORS_ORIGIN=https://your-frontend-domain.vercel.app
# Base URL where this API is reachable (used for constructing absolute asset URLs if needed)
ASSET_BASE_URL=https://your-backend-service.onrender.com
# Leave false (or remove). Set to true ONLY if you want to skip migrations.
MIGRATIONS_DISABLE=false
