// Deprecated: Original request modal (form moved to RequestPage).
// Keeping a harmless stub so existing imports (if any) don't break.
// You can safely delete this file after removing all imports.
import React from 'react';

export default function RequestModal() {
  return null;
}